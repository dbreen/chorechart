// App-wide configuration settings

// Version information
export const APP_VERSION = '1.0.2'

// Feature flags
export const FEATURES = {
  STICKERS_ENABLED: true,
  DEBUG_MODE: false
}

// App settings
export const SETTINGS = {
  ALLOW_RESET: true,
  PERSIST_STICKERS: true
}
